import styled from "styled-components";

const TopWrapper = styled.div`

  .topContent {
    padding: 1rem;
  }
  #feature-image {
    width: 100%;
    max-width: 1440px;
  }
`
export default TopWrapper;
